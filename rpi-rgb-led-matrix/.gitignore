# c++
*.o
*.a
*~
*.so

# env
.envrc
.direnv

# python
__pycache__/

# CMake writes these here (bindings/python/rgbmatrix/{core,graphics}.cpp) as
# part of `pip install .` from the repo root - regenerated from the .pyx
# source on every build, not committed, so it always matches whatever
# Cython/Python actually built it instead of going stale.
bindings/python/rgbmatrix/core.cpp
bindings/python/rgbmatrix/graphics.cpp
build/