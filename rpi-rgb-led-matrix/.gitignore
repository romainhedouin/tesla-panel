*.o
*.a
*~
*.so
# Cython output - regenerated from the .pyx source at install time (see
# pi_side_install.sh) rather than committed, so it always matches whatever
# Cython/Python is actually on the build machine instead of going stale.
bindings/python/rgbmatrix/core.cpp
bindings/python/rgbmatrix/graphics.cpp
